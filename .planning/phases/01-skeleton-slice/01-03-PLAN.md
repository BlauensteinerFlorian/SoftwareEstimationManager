---
phase: 01-skeleton-slice
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - docker-compose.yml
  - .env.example
  - .dockerignore
  - .gitignore
  - README.md
  - data/.gitkeep
  - config/.gitkeep
autonomous: false
requirements: [INFRA-01, INFRA-03, INFRA-04, INFRA-05, INFRA-06, INFRA-07]
tags: [docker-compose, nginx, healthcheck, smoke-test, walking-skeleton]
user_setup:
  - service: docker-desktop
    why: "Required to run `docker compose up` (INFRA-01). Bundled with macOS via brew --cask docker."
    env_vars: []
    dashboard_config:
      - task: "Install or start Docker Desktop with Compose v2 plugin"
        location: "https://www.docker.com/products/docker-desktop/ or brew install --cask docker"
must_haves:
  truths:
    - "User runs `docker compose up` and both services start without further manual steps"
    - "Only host port 3000 is exposed; port 8000 is NOT reachable from the host"
    - "Browser POST to /api/estimates via http://localhost:3000 returns a calculated pt number (same-origin, no CORS)"
    - "GET http://localhost:3000/api/health returns 200 with {status: ok, version: 0.1.0, timestamp: <ISO-8601 UTC>}"
    - "Bind-mounts ./data and ./config persist across docker compose down/up cycles"
    - "Backend logs show DATABASE_URL=sqlite:///./data/estimates.db at startup"
    - "Frontend startup is gated on backend healthcheck via depends_on condition: service_healthy"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Two-service orchestration, healthcheck-gated, bind-mounted"
      contains: "condition: service_healthy"
    - path: "data/.gitkeep"
      provides: "Tracks bind-mount target dir in VCS"
    - path: "config/.gitkeep"
      provides: "Tracks bind-mount target dir in VCS"
    - path: ".gitignore"
      provides: "Excludes node_modules, __pycache__, .venv, dist, data/*.db"
      contains: "data/*.db"
    - path: ".dockerignore"
      provides: "Reduces build context size; excludes node_modules, .planning, *.md"
    - path: ".env.example"
      provides: "Documents DATABASE_URL for future external env-file usage"
    - path: "README.md"
      provides: "Two-sentence first-run guide (full README is Phase 4)"
  key_links:
    - from: "docker-compose.yml"
      to: "backend/Dockerfile"
      via: "build: ./backend"
      pattern: "build: ./backend"
    - from: "docker-compose.yml"
      to: "frontend/Dockerfile"
      via: "build: ./frontend"
      pattern: "build: ./frontend"
    - from: "docker-compose.yml backend.healthcheck"
      to: "backend/app/routers/health.py"
      via: "urllib request to http://localhost:8000/api/health"
      pattern: "urllib\\.request\\.urlopen.*localhost:8000/api/health"
    - from: "docker-compose.yml frontend.depends_on"
      to: "docker-compose.yml backend.healthcheck"
      via: "condition: service_healthy"
      pattern: "condition: service_healthy"
    - from: "frontend/nginx.conf proxy_pass"
      to: "backend container (compose-internal DNS)"
      via: "Docker compose service-name DNS resolution to backend:8000"
      pattern: "proxy_pass http://backend:8000/api/"
---

<objective>
Phase 1, Plan 3: Tie the backend and frontend together with `docker-compose.yml` (two services, healthcheck-gated, bind-mounted), supporting root infrastructure files (`.gitignore`, `.dockerignore`, `.env.example`, `README.md`, `data/.gitkeep`, `config/.gitkeep`), then build the stack and prove the walking skeleton works end-to-end via the six smoke commands from 01-RESEARCH.md §"Validation Architecture" and one user browser checkpoint.

Purpose: This is the wave that proves the spine is alive. Six automated commands + one human-eyes-on browser test confirm every Phase-1 success criterion (INFRA-01 through INFRA-07 plus the four ROADMAP truths) is materially satisfied. Failure of any single command means the skeleton is broken and the phase is not done.

Output: A bootable `docker compose up` topology, persistent bind-mounts proved across container restarts, and a green smoke matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-skeleton-slice/01-CONTEXT.md
@.planning/phases/01-skeleton-slice/01-RESEARCH.md
@.planning/phases/01-skeleton-slice/01-PATTERNS.md
@.planning/phases/01-skeleton-slice/01-01-PLAN.md
@.planning/phases/01-skeleton-slice/01-02-PLAN.md

<interfaces>
<!-- Service-to-service contract this plan must establish -->

Compose service names (used as Docker DNS hostnames):
- `backend` → listens on compose-internal :8000, NOT host-exposed
- `frontend` → listens on container :80, mapped to host :3000

Bind-mount paths:
- Host `./data`  → Container `/app/data`  (backend service)
- Host `./config` → Container `/app/config` (backend service)

Environment:
- `DATABASE_URL=sqlite:///./data/estimates.db` (passed to backend; Phase 1 logged only, not used)

Healthcheck contract:
- Backend container runs: `python -c "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"`
- Exit 0 → healthy; non-zero → unhealthy
- Frontend `depends_on.backend.condition: service_healthy` blocks startup until first healthy

Network topology (per 01-RESEARCH.md §"System Architecture Diagram"):
- Browser → host:3000 → nginx (container :80) → SPA serve OR proxy_pass to backend:8000 → FastAPI
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docker-compose.yml and supporting root infrastructure files</name>
  <files>
    docker-compose.yml
    .env.example
    .dockerignore
    .gitignore
    README.md
    data/.gitkeep
    config/.gitkeep
  </files>
  <read_first>
    - 01-RESEARCH.md §"Root — docker-compose.yml" (the exact YAML block, lines ~952-984)
    - 01-RESEARCH.md §"Root — .env.example", §"Root — .gitignore", §"Root — .dockerignore", §"Root — README.md (Phase-1-Minimum)"
    - 01-CONTEXT.md §D-06 (healthcheck uses python urllib, not curl)
    - 01-PATTERNS.md §"Pattern Assignments — Root" (`docker-compose.yml`, `.gitignore`, `.dockerignore`, `data/.gitkeep`, `config/.gitkeep`, `README.md`, `.env.example`)
    - The existing `.gitignore` at repo root (currently 9 bytes per the directory listing) — to determine whether it needs to be APPENDED to or rewritten
    - CLAUDE.md §"Decision 7: Docker Compose structure" — confirms long-form depends_on + service_healthy condition + curl avoidance
  </read_first>
  <action>
    Create the root infrastructure that orchestrates the backend (Plan 01) and frontend (Plan 02) images. Implements INFRA-01 (no manual steps), INFRA-04 (only port 3000 exposed), INFRA-05 (DATABASE_URL env var), INFRA-06 (bind-mounts), INFRA-07 (healthcheck-gated frontend boot).

    1. **`docker-compose.yml`** (repo root) — copy 01-RESEARCH.md §"Root — docker-compose.yml" verbatim. Critical invariants:
       - NO `version:` key (Compose v2 schema)
       - `backend` service: `build: ./backend`, `expose: ["8000"]` (NOT `ports:` — INFRA-04), `environment: DATABASE_URL: sqlite:///./data/estimates.db`, `volumes: ["./data:/app/data", "./config:/app/config"]`, healthcheck with `test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/health')"]` (Pitfall #5 avoidance: no curl install), `interval: 10s`, `timeout: 3s`, `retries: 5`, `start_period: 5s`, `restart: unless-stopped`
       - `frontend` service: `build: ./frontend`, `ports: ["3000:80"]` (the ONLY host-port mapping), `depends_on: backend: condition: service_healthy` (long-form, Pitfall #15 avoidance), `restart: unless-stopped`
       - NO frontend healthcheck — only backend has one (the audit criterion in the orchestrator quality-gate confirms `[.[] | select(.Health == "healthy")] | length == 1` — exactly one service is healthy-marked)
       - NO `networks:` or `volumes:` top-level blocks needed — compose auto-creates a default network and the named volumes are bind-mounts already

    2. **`.env.example`** (repo root) — copy 01-RESEARCH.md §"Root — .env.example" verbatim:
       ```
       # Phase 1 setzt diese in compose direkt — .env ist optional und dokumentarisch.
       DATABASE_URL=sqlite:///./data/estimates.db
       ```

    3. **`.dockerignore`** (repo root) — copy 01-RESEARCH.md §"Root — .dockerignore" verbatim:
       ```
       **/.git
       **/.gitignore
       **/node_modules
       **/__pycache__
       **/.venv
       **/.pytest_cache
       **/dist
       **/*.md
       .planning/
       ```

    4. **`.gitignore`** (repo root) — APPEND the block from 01-RESEARCH.md §"Root — .gitignore" to the existing 9-byte file (do NOT overwrite — the current content is presumably `.claude/` from the initial commit; preserve it). Final file should contain ALL of:
       ```
       # Python
       __pycache__/
       *.py[cod]
       .venv/
       .uv/

       # Frontend
       node_modules/
       frontend/dist/
       *.tsbuildinfo

       # Daten (Phase 3+)
       data/*.db
       data/*.db-journal
       data/*.db-wal
       data/*.db-shm

       # OS / Editor
       .DS_Store
       .idea/
       .vscode/
       ```
       Plus whatever existed before (e.g., `.claude/`). De-duplicate entries if the existing file already contained any of these.

       CRITICAL: `data/*.db` must be on a separate line from `data/.gitkeep` so the `.gitkeep` file IS tracked (no leading wildcard match — see PATTERNS §`.gitignore` "Critical constraint"). The pattern `data/*.db` does NOT match `.gitkeep` because `.gitkeep` has no `.db` extension.

    5. **`README.md`** (repo root) — copy 01-RESEARCH.md §"Root — README.md (Phase-1-Minimum)" verbatim:
       ```
       # Software-Aufwandsschätzung

       **Voraussetzungen:** Docker Desktop mit Docker Compose v2 (Aufruf via `docker compose`, nicht `docker-compose`).

       **Start:** `docker compose up` — danach öffne http://localhost:3000.
       ```
       Do NOT pad with installation steps, troubleshooting, or PostgreSQL migration notes — those are Phase 4 deliverables (INFRA-10).

    6. **`data/.gitkeep`** and **`config/.gitkeep`** — empty files. Run `mkdir -p data config && touch data/.gitkeep config/.gitkeep`. These create the bind-mount target directories on the host and make them VCS-trackable. Without these, the first `docker compose up` on a fresh checkout could create the dirs with non-deterministic ownership.
  </action>
  <verify>
    <automated>test -f docker-compose.yml && test -f .env.example && test -f .dockerignore && test -f .gitignore && test -f README.md && test -f data/.gitkeep && test -f config/.gitkeep && ! grep -q '^version:' docker-compose.yml && grep -q '^services:' docker-compose.yml && grep -q 'build: ./backend' docker-compose.yml && grep -q 'build: ./frontend' docker-compose.yml && grep -q 'expose:' docker-compose.yml && grep -q '"8000"' docker-compose.yml && ! grep -E '^\s*-\s*"8000:8000"' docker-compose.yml && grep -q '"3000:80"' docker-compose.yml && grep -q 'condition: service_healthy' docker-compose.yml && grep -q "urllib.request.urlopen('http://localhost:8000/api/health')" docker-compose.yml && grep -q 'DATABASE_URL: sqlite:///./data/estimates.db' docker-compose.yml && grep -q './data:/app/data' docker-compose.yml && grep -q './config:/app/config' docker-compose.yml && grep -q 'docker compose up' README.md && grep -q 'node_modules' .gitignore && grep -q 'data/\*\.db' .gitignore && grep -q '\*\*/node_modules' .dockerignore && grep -q '\.planning/' .dockerignore</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` exists, has NO `version:` line, defines exactly two services `backend` and `frontend`
    - `docker-compose.yml` backend service uses `expose: ["8000"]` and does NOT contain `ports: - "8000:8000"` or `ports: - "8000"` — verify by grep
    - `docker-compose.yml` frontend service has `ports: - "3000:80"` (the only host-port mapping)
    - `docker-compose.yml` has `depends_on: backend: condition: service_healthy` (long-form)
    - `docker-compose.yml` healthcheck test command contains `urllib.request.urlopen('http://localhost:8000/api/health')` and does NOT contain `curl`
    - `docker-compose.yml` declares `DATABASE_URL: sqlite:///./data/estimates.db` in backend `environment:`
    - `docker-compose.yml` declares bind-mounts `./data:/app/data` and `./config:/app/config`
    - `.gitignore` contains `node_modules/`, `__pycache__/`, `.venv/`, `frontend/dist/`, and `data/*.db` patterns
    - `.gitignore` does NOT ignore `data/.gitkeep` or `config/.gitkeep`: `git check-ignore data/.gitkeep config/.gitkeep` exits non-zero
    - `.dockerignore` contains `**/node_modules`, `**/__pycache__`, `**/.venv`, `.planning/`, `**/*.md`
    - `README.md` contains the two-sentence German first-run guide referencing `docker compose up` and http://localhost:3000
    - `data/.gitkeep` and `config/.gitkeep` both exist as empty files
    - The phrase `Reset`, `Backup`, `PostgreSQL`, or `Migration` does NOT appear in README.md (those are Phase 4)
  </acceptance_criteria>
  <done>Root infrastructure ready; the next `docker compose up --build` should successfully orchestrate both services with healthcheck-gating.</done>
</task>

<task type="auto">
  <name>Task 2: Build, boot, and run the six-command smoke matrix end-to-end</name>
  <files>(no source-file changes — orchestration + verification only; may create data/test-marker and config/test-marker temporarily and then remove them)</files>
  <read_first>
    - 01-RESEARCH.md §"Validation Architecture" → "Phase Requirements → Test Map" (the six command rows)
    - 01-RESEARCH.md §"Common Pitfalls" → Pitfall #4 (depends_on without service_healthy)
    - 01-CONTEXT.md §"decisions" D-06 (health payload contract)
    - docker-compose.yml, frontend/nginx.conf, backend/app/routers/health.py, backend/app/routers/estimates.py (all created in prior plans/tasks) to confirm they are present before launching
  </read_first>
  <action>
    Build the images, start the compose stack with `--wait` for healthcheck-gated readiness, and execute the six automated smoke commands from 01-RESEARCH.md §"Phase Requirements → Test Map" plus the bind-mount persistence test. All commands run from the repo root.

    Step 1 — Confirm Docker is reachable:
    ```
    docker version --format '{{.Server.Version}}' || echo "Docker daemon not running"
    docker compose version | head -1
    ```
    If Docker is not running, surface as a blocker (the user must start Docker Desktop). This is a host precondition documented in README.md and the user_setup frontmatter.

    Step 2 — Build and start the stack with healthcheck-gating:
    ```
    docker compose up -d --build --wait --wait-timeout 120
    ```
    `--wait` blocks until services are healthy (or unhealthy). `--wait-timeout 120` gives 120s — generous since first-build pulls base images. INFRA-01 success criterion: this command must exit 0 with no interactive prompts.

    Step 3 — Run the six smoke commands SEQUENTIALLY. Each must succeed before proceeding.

    **SMOKE-01 (INFRA-01: services running):**
    ```
    docker compose ps --format json | jq -e 'all(.State == "running")'
    ```

    **SMOKE-02 (INFRA-03: nginx reverse-proxy works end-to-end):**
    ```
    curl -fsS -X POST http://localhost:3000/api/estimates \
      -H 'Content-Type: application/json' \
      -d '{"pages":10,"complexity":"medium"}' | jq -e '.pt == 15'
    ```
    Expected: `pt = 10 × 1.5 = 15` per the skeleton multiplier map (D-03).

    **SMOKE-03 (INFRA-04: backend NOT host-exposed):**
    ```
    ! curl -fsS --max-time 2 http://localhost:8000/api/health 2>/dev/null
    ```
    This command must FAIL (connection refused). The `!` inverts the exit code; the overall step exits 0 only when curl fails. If curl succeeds, INFRA-04 is violated — the backend port is leaking to the host.

    **SMOKE-04 (INFRA-05: backend logs DATABASE_URL with SQLite default):**
    ```
    docker compose logs backend | grep -q 'DATABASE_URL=sqlite:///./data/estimates.db'
    ```

    **SMOKE-05 (INFRA-06: bind-mount persistence):**
    ```
    touch data/test-marker config/test-marker
    docker compose restart backend
    docker compose exec backend test -f /app/data/test-marker
    docker compose exec backend test -f /app/config/test-marker
    rm data/test-marker config/test-marker
    ```
    Verifies the bind-mount is actually mounted (file created on host visible in container after restart). The `restart` exercises the mount-survives-container-restart guarantee.

    **SMOKE-06 (INFRA-07: /api/health 200 with correct payload + backend healthcheck status):**
    ```
    curl -fsS http://localhost:3000/api/health \
      | jq -e '.status == "ok" and (.timestamp | length > 0) and .version == "0.1.0"'
    docker inspect "$(docker compose ps -q backend)" --format '{{.State.Health.Status}}' | grep -q '^healthy$'
    ```
    First subcommand verifies the D-06 payload contract. Second verifies the Docker healthcheck has actually transitioned to `healthy` state (not just `starting`).

    Step 4 — Capture status for the SUMMARY:
    ```
    docker compose ps --format json > /tmp/01-03-compose-ps.json
    docker inspect "$(docker compose ps -q backend)" --format '{{json .State.Health}}' > /tmp/01-03-backend-health.json
    docker compose logs --tail=40 backend > /tmp/01-03-backend-logs.txt
    docker compose logs --tail=20 frontend > /tmp/01-03-frontend-logs.txt
    ```
    These captures go into the SUMMARY for the user to review. They are NOT committed to VCS.

    Step 5 — Leave the stack running (`-d` detached) for the next task's browser checkpoint. Do NOT run `docker compose down` here.

    If ANY smoke command fails: STOP, dump `docker compose logs --tail=100 backend frontend`, surface the failure clearly in the executor's output, and let the user diagnose. Do not "fix forward" by patching unrelated files — a failed smoke means the skeleton is broken at the topology level and the prior plans need a checker pass.
  </action>
  <verify>
    <automated>docker compose ps --format json | jq -e 'all(.State == "running")' && curl -fsS -X POST http://localhost:3000/api/estimates -H 'Content-Type: application/json' -d '{"pages":10,"complexity":"medium"}' | jq -e '.pt == 15' && ! curl -fsS --max-time 2 http://localhost:8000/api/health 2>/dev/null && docker compose logs backend | grep -q 'DATABASE_URL=sqlite:///./data/estimates.db' && curl -fsS http://localhost:3000/api/health | jq -e '.status == "ok" and (.timestamp | length > 0) and .version == "0.1.0"' && docker inspect "$(docker compose ps -q backend)" --format '{{.State.Health.Status}}' | grep -q '^healthy$' && docker compose ps --format json | jq -e '[.[] | select(.Health == "healthy")] | length == 1'</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose up -d --build --wait --wait-timeout 120` exits 0 within 120s
    - SMOKE-01: `docker compose ps --format json | jq -e 'all(.State == "running")'` exits 0
    - SMOKE-02: POST to `http://localhost:3000/api/estimates` with `{"pages":10,"complexity":"medium"}` returns `{"pt": 15}` (or `15.0`); `jq -e '.pt == 15'` exits 0
    - SMOKE-03: `curl --max-time 2 http://localhost:8000/api/health` FAILS (connection refused on host); the `!` inversion makes the overall step exit 0
    - SMOKE-04: `docker compose logs backend` contains the literal string `DATABASE_URL=sqlite:///./data/estimates.db`
    - SMOKE-05: After `touch data/test-marker config/test-marker` + `docker compose restart backend`, the files are visible inside the container at `/app/data/test-marker` and `/app/config/test-marker`; files are then cleaned up
    - SMOKE-06a: `curl http://localhost:3000/api/health` returns 200 with JSON satisfying `.status == "ok" and (.timestamp | length > 0) and .version == "0.1.0"`
    - SMOKE-06b: `docker inspect $(docker compose ps -q backend) --format '{{.State.Health.Status}}'` returns `healthy`
    - Audit: `docker compose ps --format json | jq -e '[.[] | select(.Health == "healthy")] | length == 1'` exits 0 — exactly ONE service (backend) is health-marked; frontend has NO healthcheck declared
    - All six logs/inspect outputs captured to `/tmp/01-03-*` for SUMMARY reference
    - Stack remains running (detached) at end of task for the browser checkpoint
  </acceptance_criteria>
  <done>All six smoke commands green; bind-mount persistence proven; backend port confirmed not host-exposed; healthcheck status confirmed `healthy`. Stack is live and ready for the human browser checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser checkpoint — end-to-end form round-trip</name>
  <files>(no file changes — manual visual + interaction verification)</files>
  <read_first>
    - ROADMAP.md §"Phase 1: Skeleton Slice" — Success Criterion 2 ("Nutzer öffnet http://localhost:3000 im Browser, sendet via Eingabe-Form einen POST an `/api/estimates` ... und sieht eine vom Backend berechnete Zahl gerendert")
    - 01-RESEARCH.md §"Validation Architecture" → "Manuelle Verifikation" note (browser test is the only manual gate; headless playwright is out-of-scope for Phase 1)
    - 01-RESEARCH.md §"System Architecture Diagram" Datenfluss-Trace (steps 1-9) — describes what the user should see
  </read_first>
  <what-built>
    The walking skeleton is complete and live. The user can now visually verify the end-to-end round-trip in their browser. This is the ONLY manual gate in Phase 1 — the six smoke commands in Task 2 covered everything automatable.
  </what-built>
  <action>
    Pause execution and present the browser-verification protocol below to the user. Do not proceed past this task until the user types "approved" or describes a failure mode. This is a `checkpoint:human-verify` gate — Claude has automated everything it can; the remaining verification is visual.
  </action>
  <how-to-verify>
    1. Confirm the stack is still running (Task 2 left it detached):
       ```
       docker compose ps
       ```
       Both services should be `Up`, backend marked `(healthy)`.

    2. Open `http://localhost:3000` in your default browser.

    3. **Expected initial render:**
       - Page title in tab: "Software-Aufwandsschätzung"
       - Heading: "Software-Aufwandsschätzung"
       - Subtitle: "Skeleton-Phase: ein Parameter, eine Zahl."
       - Form with two visible inputs:
         - "Anzahl Pages" (number input, empty)
         - "Komplexität" (select dropdown showing "— bitte wählen —" with no other option pre-selected; this is the Pitfall #17 anti-anchoring pattern)
       - Submit button labeled "Berechnen"
       - No result panel visible yet (`pt === null` returns null)

    4. **Anti-anchoring spot-check:** Click into "Komplexität" — verify the placeholder "— bitte wählen —" is greyed out / disabled and the dropdown lists four options: `low`, `medium`, `high`, `very_high`. No option is pre-selected.

    5. **Happy-path round-trip:**
       - Enter `10` into "Anzahl Pages"
       - Select `medium` from "Komplexität"
       - Click "Berechnen"
       - **Expected result:** A green-bordered panel appears below the form containing the text `Aufwand: 15 PT` (because 10 × 1.5 = 15.0; React renders via `String(pt)` so the display is `15` — `String(15.0) === "15"` in JS, no decimal point).

    6. **Error-path spot-check (validation):**
       - Reload the page.
       - Leave "Anzahl Pages" blank and select `low`.
       - Click "Berechnen".
       - Expected: A red error message "Bitte alle Felder ausfüllen." appears. No result panel.

    7. **Network spot-check (Pitfall #2 sanity):** Open DevTools → Network tab. Submit the happy path again. The request should target `/api/estimates` (relative URL via Same-Origin nginx proxy), NOT `http://localhost:8000/api/estimates`. The response should be `200 OK` with body `{"pt": 15.0}` (or similar; FastAPI may serialize as `15.0` or `15`).

    8. **Tear-down (optional cleanup):** When done, stop the stack with `docker compose down`. The bind-mount target dirs `./data` and `./config` remain on disk (because of `.gitkeep`).

    9. **Type "approved"** to confirm all four checks (initial render, happy path, error path, relative URL) passed. If any check fails, describe the failure mode (screenshot, browser console error, network tab observation) so the executor can route the gap to a follow-up plan.
  </how-to-verify>
  <verify>
    <human-check>User has performed checks 3–7 in a browser and typed "approved" (or described the specific failure mode). No automated command can replace human eyes on the rendered DOM, the dropdown's greyed-out placeholder, and the DevTools network panel.</human-check>
  </verify>
  <resume-signal>Type "approved" if all checks passed, or describe the specific failure mode (which check, observed vs expected) so the planner can scope a gap-closure plan.</resume-signal>
  <done>User has typed "approved", confirming initial render, anti-anchoring placeholder, happy-path round-trip, error-path validation, and relative-URL network behavior all match expectations. Phase 1 success criterion 2 (ROADMAP) is materially verified by human inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host shell → docker daemon | Trusted (developer's own machine) |
| Compose internal network → bind-mount filesystem | Container root user writes; Pitfall #14 accepted for v1 |
| Browser → host:3000 | This is the SOLE attack surface in v1; everything else is compose-internal |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Backend port 8000 leaked to host | mitigate | `expose: ["8000"]` (no `ports:`) on backend service in docker-compose.yml. SMOKE-03 actively verifies via `! curl localhost:8000`. |
| T-03-02 | Denial of Service | Frontend starts before backend ready → user sees 502 | mitigate | `depends_on.backend.condition: service_healthy` (long-form). Pitfall #15 prevention. SMOKE-06b verifies healthcheck transitioned to `healthy`. |
| T-03-03 | Spoofing | Compose service-name DNS spoofing | accept | Docker compose default network isolates services; no external attacker on the bridge. |
| T-03-04 | Tampering | Docker base images (python:3.12-slim-bookworm, node:22-alpine, nginx:1.27-alpine, ghcr.io/astral-sh/uv:0.11.14) | accept | Official Docker Hub images + GitHub Container Registry official Astral image. Pinned by tag (not floating `latest`). |
| T-03-05 | Repudiation | No structured audit log of compose actions | accept | v1 is local-first single-user; no audit requirement. `docker compose logs` is sufficient. |
| T-03-SC | Tampering | Base image supply chain | accept | All images sourced from canonical registries (docker.io/library, ghcr.io/astral-sh) with pinned tags. |
</threat_model>

<verification>
End-to-end phase verification (run from repo root after all three tasks complete):

```bash
# Compose orchestration health
docker compose ps --format json | jq '[.[] | {Service, State, Health}]'

# All six smoke commands (Task 2 verify clause is the canonical block; reproduce here for SUMMARY)
docker compose ps --format json | jq -e 'all(.State == "running")'
curl -fsS -X POST http://localhost:3000/api/estimates -H 'Content-Type: application/json' -d '{"pages":10,"complexity":"medium"}' | jq -e '.pt == 15'
! curl -fsS --max-time 2 http://localhost:8000/api/health 2>/dev/null
docker compose logs backend | grep -q 'DATABASE_URL=sqlite:///./data/estimates.db'
curl -fsS http://localhost:3000/api/health | jq -e '.status == "ok" and (.timestamp | length > 0) and .version == "0.1.0"'
docker inspect "$(docker compose ps -q backend)" --format '{{.State.Health.Status}}' | grep -q '^healthy$'

# Healthcheck audit (exactly ONE service healthy — frontend has none)
docker compose ps --format json | jq -e '[.[] | select(.Health == "healthy")] | length == 1'

# Bind-mount marker survives down/up (additional persistence test)
touch data/persist-test
docker compose down
docker compose up -d --wait
docker compose exec backend test -f /app/data/persist-test && echo "bind-mount persisted across down/up"
rm data/persist-test
```
</verification>

<success_criteria>
- `docker compose up` (or `up --build --wait`) starts both services with zero manual intervention (INFRA-01)
- nginx reverse-proxy delivers `{pages:10, complexity:"medium"}` → `{pt:15}` round-trip via http://localhost:3000 (INFRA-03 + ROADMAP Goal 2)
- Backend port 8000 NOT reachable from host (INFRA-04 + ROADMAP Goal 1)
- Backend logs DATABASE_URL on startup with SQLite default; bind-mounts persistent across restart (INFRA-05 + INFRA-06 + ROADMAP Goal 3)
- `/api/health` returns 200 with D-06 payload; frontend startup is gated on backend `service_healthy` (INFRA-07 + ROADMAP Goal 4)
- Human browser check: form renders, happy-path produces "Aufwand: 15 PT", error-path shows German validation, relative URL in network tab
- All four ROADMAP §"Phase 1: Skeleton Slice" success criteria materially satisfied
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-slice/01-03-SUMMARY.md` when done. Include:
- The full six-smoke-command output (paste exit codes + jq outputs)
- The captured `/tmp/01-03-backend-logs.txt` (last 40 lines)
- The captured `/tmp/01-03-backend-health.json`
- Confirmation of the human browser checkpoint outcome ("approved" or failure mode)
- Note any peer-warning or pull-time observations from the first `--build` run
</output>
