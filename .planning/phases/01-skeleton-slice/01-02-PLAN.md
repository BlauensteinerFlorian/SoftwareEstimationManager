---
phase: 01-skeleton-slice
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/pnpm-lock.yaml
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/vite.config.ts
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/api/client.ts
  - frontend/src/components/EstimateForm.tsx
  - frontend/src/components/ResultPanel.tsx
  - frontend/nginx.conf
  - frontend/Dockerfile
autonomous: true
requirements: [INFRA-03]
tags: [vite, react19, tailwind4, nginx, walking-skeleton]
must_haves:
  truths:
    - "frontend/package.json pins vite to ~7.3.x (NOT 8.x), @vitejs/plugin-react to ^5.2.x (NOT 6.x), typescript to ~5.9.x (NOT 6.x), react to ^19.2.x"
    - "frontend/pnpm-lock.yaml exists and is consistent with frontend/package.json"
    - "frontend/Dockerfile is multi-stage: node:22-alpine build → nginx:1.27-alpine serve"
    - "frontend/nginx.conf has location /api/ → proxy_pass http://backend:8000/api/ and SPA fallback try_files $uri $uri/ /index.html"
    - "frontend/src/api/client.ts uses relative /api/estimates URL (no hardcoded http://localhost:8000)"
    - "frontend/src/components/EstimateForm.tsx complexity dropdown has placeholder option value='' with disabled attribute (Pitfall #17 anti-anchoring)"
    - "frontend/src/components/ResultPanel.tsx renders Aufwand: {String(pt)} PT with no Intl.NumberFormat (Phase 2 will add it)"
  artifacts:
    - path: "frontend/Dockerfile"
      provides: "Multi-stage build → nginx serve"
      contains: "FROM nginx:1.27-alpine"
    - path: "frontend/nginx.conf"
      provides: "SPA static serve + /api/ reverse-proxy to backend:8000"
      contains: "proxy_pass http://backend:8000/api/"
    - path: "frontend/package.json"
      provides: "Frontend dep manifest with Vite-7-compatible plugin versions"
      contains: "\"vite\": \"^7.3"
    - path: "frontend/pnpm-lock.yaml"
      provides: "Pinned dependency graph"
    - path: "frontend/vite.config.ts"
      provides: "@vitejs/plugin-react + @tailwindcss/vite + dev proxy /api → :8000"
    - path: "frontend/src/api/client.ts"
      provides: "Typed postEstimate(req) fetch wrapper"
      exports: ["postEstimate", "Complexity", "EstimateRequest", "EstimateResponse"]
    - path: "frontend/src/components/EstimateForm.tsx"
      provides: "pages + complexity inputs, submit, error display"
      exports: ["default (EstimateForm)"]
    - path: "frontend/src/components/ResultPanel.tsx"
      provides: "Conditional Aufwand: N PT rendering"
      exports: ["default (ResultPanel)"]
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "backend POST /api/estimates"
      via: "fetch('/api/estimates', {method:'POST', body: JSON.stringify({pages, complexity})})"
      pattern: "fetch\\('/api/estimates'"
    - from: "frontend/nginx.conf"
      to: "backend:8000"
      via: "proxy_pass on /api/"
      pattern: "proxy_pass http://backend:8000/api/"
    - from: "frontend/src/components/EstimateForm.tsx"
      to: "frontend/src/api/client.ts"
      via: "import postEstimate"
      pattern: "import.*postEstimate.*from.*api/client"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/{EstimateForm,ResultPanel}.tsx"
      via: "import default exports, render in shell layout"
      pattern: "import EstimateForm from.*components/EstimateForm"
---

<objective>
Phase 1, Plan 2: Scaffold the entire `frontend/` directory — five TypeScript source files per D-05 (main.tsx, App.tsx, api/client.ts, components/EstimateForm.tsx, components/ResultPanel.tsx) plus configuration (Vite, TS, Tailwind v4, nginx) and a multi-stage Dockerfile that builds the SPA and serves it via nginx.

Purpose: This is the user-visible half of the walking skeleton. The form (D-01: pages + complexity, no defaults) submits to the relative `/api/estimates` URL, the nginx reverse-proxy forwards to `backend:8000` via Docker DNS, and the response renders as `Aufwand: N PT`. Plain `fetch` + `useState` only — no react-hook-form/zod/TanStack/shadcn (all deferred to Phase 2). Bare Tailwind utility classes on native HTML elements.

Output: A fully buildable `frontend/` directory with locked Vite-7-compatible versions, ready for Plan 03 to orchestrate the compose build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@.planning/phases/01-skeleton-slice/01-CONTEXT.md
@.planning/phases/01-skeleton-slice/01-RESEARCH.md
@.planning/phases/01-skeleton-slice/01-PATTERNS.md
@.planning/phases/01-skeleton-slice/01-01-PLAN.md

<interfaces>
<!-- Cross-language contract: frontend MUST mirror backend Pydantic schemas exactly -->

Mirrors backend `app/schemas/estimate.py` (created in Plan 01):

```typescript
// Must match Pydantic Literal["low", "medium", "high", "very_high"]
export type Complexity = 'low' | 'medium' | 'high' | 'very_high'

// Must match Pydantic EstimateRequest
export interface EstimateRequest {
  pages: number        // backend validates ge=0, le=10_000
  complexity: Complexity
}

// Must match Pydantic EstimateResponse
export interface EstimateResponse {
  pt: number           // backend serializes as JSON number (Phase 1 float; Phase 2 will use Decimal-as-string)
}

// HTTP contract (D-02):
// POST /api/estimates  →  { pages: int, complexity: "low" | "medium" | "high" | "very_high" }
//                    ←  { pt: number }
```

Skeleton math (Plan 01 backend): `pt = pages × {low: 1.0, medium: 1.5, high: 2.5, very_high: 4.0}[complexity]`
Example: `{pages: 10, complexity: "medium"}` → `{pt: 15.0}`. This example MUST work in Plan 03's smoke test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scaffold frontend/ source tree, configs, nginx, and Dockerfile</name>
  <files>
    frontend/package.json
    frontend/tsconfig.json
    frontend/tsconfig.node.json
    frontend/vite.config.ts
    frontend/index.html
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/index.css
    frontend/src/api/client.ts
    frontend/src/components/EstimateForm.tsx
    frontend/src/components/ResultPanel.tsx
    frontend/nginx.conf
    frontend/Dockerfile
  </files>
  <read_first>
    - 01-RESEARCH.md §"Code Examples" → all Frontend code blocks (lines ~646-950): `package.json`, `vite.config.ts`, `src/index.css`, `src/main.tsx`, `src/App.tsx`, `src/api/client.ts`, `src/components/EstimateForm.tsx`, `src/components/ResultPanel.tsx`, `index.html`, `Dockerfile`, `nginx.conf`
    - 01-RESEARCH.md §"Common Pitfalls" → Pitfall #1 (Vite-Plugin-Version) — exact peer-dep rule that pins `@vitejs/plugin-react@^5.2.0` (NOT 6.x) and `vite@^7.3.3` (NOT 8.x)
    - 01-RESEARCH.md §"Common Pitfalls" → Pitfall #2 (relative /api/ URLs only, no hardcoded localhost:8000)
    - 01-RESEARCH.md §"Common Pitfalls" → Pitfall #6 (SPA-Routing-Fallback via `try_files $uri $uri/ /index.html`)
    - 01-CONTEXT.md §"Decisions" D-01 (form fields), D-05 (frontend layout), D-Discretion (pnpm + minimal Tooling + bare Tailwind utility classes)
    - 01-PATTERNS.md §"Pattern Assignments — Frontend" (all twelve file entries with their "Critical constraints")
    - CLAUDE.md §"Frontend (package.json)" version pins
  </read_first>
  <action>
    Create the full `frontend/` directory tree exactly as specified in 01-RESEARCH.md §"Code Examples" and 01-PATTERNS.md §"Pattern Assignments — Frontend". This implements INFRA-03 (nginx reverse-proxy `/api/*` → backend:8000), D-01 (pages + complexity form, no defaults), D-05 (5 TypeScript files), and the relative-URL pattern (Pitfall #2).

    Lift the code from 01-RESEARCH.md verbatim for the canonical files. Two files need standard Vite-template content because RESEARCH.md does not include them as literal blocks — use the project-references pattern from 01-PATTERNS.md §"frontend/tsconfig.json".

    File-by-file (all paths relative to repo root):

    1. **`frontend/package.json`** — copy 01-RESEARCH.md §"Frontend — package.json" verbatim. Pins MUST be exactly: `"vite": "^7.3.3"`, `"@vitejs/plugin-react": "^5.2.0"`, `"typescript": "~5.9.0"`, `"react": "^19.2.6"`, `"react-dom": "^19.2.6"`, `"@tailwindcss/vite": "^4.3.0"`, `"tailwindcss": "^4.3.0"`, `"@types/react": "^19.2.0"`, `"@types/react-dom": "^19.2.0"`. Scripts: `"dev": "vite"`, `"build": "tsc -b && vite build"`, `"preview": "vite preview --port 4173"`. `"type": "module"`, `"private": true`, `"version": "0.1.0"`.

    2. **`frontend/tsconfig.json`** — use the project-references variant from 01-PATTERNS.md §`tsconfig.json`:
       ```jsonc
       {
         "files": [],
         "references": [
           { "path": "./tsconfig.app.json" },
           { "path": "./tsconfig.node.json" }
         ]
       }
       ```
       Then ALSO create `frontend/tsconfig.app.json` with `compilerOptions` for browser context: `target: "ES2022"`, `lib: ["ES2022", "DOM", "DOM.Iterable"]`, `module: "ESNext"`, `moduleResolution: "bundler"`, `jsx: "react-jsx"`, `strict: true`, `isolatedModules: true`, `noEmit: true`, `allowSyntheticDefaultImports: true`, `esModuleInterop: true`, plus `"include": ["src"]`. (NOTE: the project-references pattern requires three files: `tsconfig.json`, `tsconfig.app.json`, `tsconfig.node.json`. The frontmatter listing only mentions tsconfig.json + tsconfig.node.json — add `frontend/tsconfig.app.json` as a sibling. If the executor prefers the alternative "single tsconfig.json with compilerOptions" variant from 01-PATTERNS.md, that is also acceptable — but then DELETE the `"references"` block and put `compilerOptions` directly in `tsconfig.json`, and DO NOT create `tsconfig.app.json`.)

    3. **`frontend/tsconfig.node.json`** — copy 01-PATTERNS.md §`tsconfig.node.json` block verbatim (target ES2022, lib `["ES2023"]`, include `["vite.config.ts"]`, strict, noEmit).

    4. **`frontend/vite.config.ts`** — copy 01-RESEARCH.md §"Frontend — vite.config.ts" verbatim. MUST include `@vitejs/plugin-react`, `@tailwindcss/vite`, and the dev proxy `server.proxy['/api'] = { target: 'http://localhost:8000', changeOrigin: false }` (Pitfall #2).

    5. **`frontend/index.html`** — copy 01-RESEARCH.md §"Frontend — index.html" verbatim. `<html lang="de">`, `<title>Software-Aufwandsschätzung</title>`, script src `/src/main.tsx`.

    6. **`frontend/src/main.tsx`** — copy 01-RESEARCH.md §"Frontend — src/main.tsx" verbatim. `createRoot(document.getElementById('root')!).render(<StrictMode><App /></StrictMode>)` + `import './index.css'`.

    7. **`frontend/src/App.tsx`** — copy 01-RESEARCH.md §"Frontend — src/App.tsx" verbatim. Single root component with `useState<number | null>(null)` for `pt`, renders shell with `<h1>Software-Aufwandsschätzung</h1>` + `<EstimateForm onResult={setPt}/>` + `<ResultPanel pt={pt}/>`. No router. Bare Tailwind classes only (`min-h-screen bg-gray-50 p-8`, `max-w-xl mx-auto bg-white rounded shadow p-6 space-y-4`).

    8. **`frontend/src/index.css`** — exactly one line: `@import "tailwindcss";` (Tailwind v4 single-import pattern).

    9. **`frontend/src/api/client.ts`** — copy 01-RESEARCH.md §"Frontend — src/api/client.ts" verbatim. Exports `Complexity`, `EstimateRequest`, `EstimateResponse`, and `async function postEstimate(req)`. MUST use relative URL `'/api/estimates'` (NEVER `http://localhost:8000/...` — Pitfall #2). Error message includes status code and German fallback.

    10. **`frontend/src/components/EstimateForm.tsx`** — copy 01-RESEARCH.md §"Frontend — src/components/EstimateForm.tsx" verbatim. Critical D-01 + Pitfall #17 details:
        - `pages` state: `useState<number | ''>('')` (NOT `useState(0)` — empty string forces explicit entry)
        - `complexity` state: `useState<Complexity | ''>('')` (NOT a default like `'medium'`)
        - The `<select>` MUST have `<option value="" disabled>— bitte wählen —</option>` as the first option
        - Validation: `if (pages === '' || complexity === '') { setError('Bitte alle Felder ausfüllen.'); return; }`
        - Submit button text: `'Berechne…'` while busy, `'Berechnen'` otherwise
        - Error display: `{error && <p className="text-sm text-red-600">{error}</p>}`

    11. **`frontend/src/components/ResultPanel.tsx`** — copy 01-RESEARCH.md §"Frontend — src/components/ResultPanel.tsx" verbatim. Returns `null` when `pt === null`. Otherwise renders `<p className="text-lg">Aufwand: {String(pt)} PT</p>` inside a green-bordered div. NO `Intl.NumberFormat` — that is Phase 2 work per D-Specifics.

    12. **`frontend/nginx.conf`** — copy 01-RESEARCH.md §"Frontend — nginx.conf" verbatim. Single server block, `listen 80`, `root /usr/share/nginx/html`, `location /` with `try_files $uri $uri/ /index.html` (SPA fallback per Pitfall #6), `location /api/` with `proxy_pass http://backend:8000/api/` (trailing slashes preserved) + standard proxy_set_header chain + `client_max_body_size 1m` + `proxy_read_timeout 30s`. No CORS headers.

    13. **`frontend/Dockerfile`** — copy 01-RESEARCH.md §"Frontend — Dockerfile (multi-stage)" verbatim. Build stage `FROM node:22-alpine AS builder`, `corepack enable && pnpm install --frozen-lockfile`, copy configs + src, `pnpm build`. Runtime stage `FROM nginx:1.27-alpine`, copy `/app/dist` to `/usr/share/nginx/html`, copy `nginx.conf` to `/etc/nginx/conf.d/default.conf`, `EXPOSE 80`. Default nginx CMD is preserved (do NOT add a CMD line).

    Code language is English; UI strings (`<h1>Software-Aufwandsschätzung</h1>`, "Anzahl Pages", "Komplexität", "Berechnen", "Berechne…", "Bitte alle Felder ausfüllen.", "— bitte wählen —", "Aufwand: {pt} PT") are German per CLAUDE.md. The placeholder text "Skeleton-Phase: ein Parameter, eine Zahl." in App.tsx must be preserved as-is.
  </action>
  <verify>
    <automated>test -f frontend/package.json && test -f frontend/Dockerfile && test -f frontend/nginx.conf && test -f frontend/vite.config.ts && test -f frontend/index.html && test -f frontend/src/main.tsx && test -f frontend/src/App.tsx && test -f frontend/src/index.css && test -f frontend/src/api/client.ts && test -f frontend/src/components/EstimateForm.tsx && test -f frontend/src/components/ResultPanel.tsx && test -f frontend/tsconfig.json && test -f frontend/tsconfig.node.json && grep -q '"vite": "\^7' frontend/package.json && ! grep -q '"vite": "\^8' frontend/package.json && grep -q '"@vitejs/plugin-react": "\^5' frontend/package.json && ! grep -q '"@vitejs/plugin-react": "\^6' frontend/package.json && grep -q '"typescript": "~5\.9' frontend/package.json && ! grep -q '"typescript": "\^6' frontend/package.json && grep -q '"react": "\^19' frontend/package.json && grep -q 'proxy_pass http://backend:8000/api/' frontend/nginx.conf && grep -q 'try_files \$uri \$uri/ /index.html' frontend/nginx.conf && grep -q "fetch('/api/estimates'" frontend/src/api/client.ts && ! grep -q 'http://localhost:8000' frontend/src/api/client.ts && grep -q 'FROM node:22-alpine AS builder' frontend/Dockerfile && grep -q 'FROM nginx:1.27-alpine' frontend/Dockerfile && grep -q 'option value="" disabled' frontend/src/components/EstimateForm.tsx && grep -q 'String(pt)' frontend/src/components/ResultPanel.tsx && ! grep -q 'Intl\.NumberFormat' frontend/src/components/ResultPanel.tsx && grep -q '@import "tailwindcss"' frontend/src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` declares `"vite": "^7.3.3"` (NOT 8.x); `grep '"vite": "\^8' frontend/package.json` returns nothing
    - `frontend/package.json` declares `"@vitejs/plugin-react": "^5.2.0"` (NOT 6.x); `grep '"@vitejs/plugin-react": "\^6' frontend/package.json` returns nothing
    - `frontend/package.json` declares `"typescript": "~5.9.0"` (NOT 6.x); `grep '"typescript": "\^6' frontend/package.json` returns nothing
    - `frontend/package.json` declares `"react": "^19.2.6"` and `"react-dom": "^19.2.6"`
    - `frontend/package.json` declares `"tailwindcss": "^4.3.0"` and `"@tailwindcss/vite": "^4.3.0"`
    - `frontend/Dockerfile` starts the build stage with `FROM node:22-alpine AS builder`
    - `frontend/Dockerfile` runtime stage is `FROM nginx:1.27-alpine` and copies `nginx.conf` to `/etc/nginx/conf.d/default.conf`
    - `frontend/nginx.conf` contains `proxy_pass http://backend:8000/api/;` (trailing slash on both sides)
    - `frontend/nginx.conf` contains `try_files $uri $uri/ /index.html;` inside `location /`
    - `frontend/nginx.conf` contains `client_max_body_size 1m;`
    - `frontend/src/api/client.ts` uses `fetch('/api/estimates', ...)` — relative URL only
    - `frontend/src/api/client.ts` does NOT contain `http://localhost:8000` or `VITE_API_URL`
    - `frontend/src/components/EstimateForm.tsx` has `<option value="" disabled>` for the complexity placeholder
    - `frontend/src/components/EstimateForm.tsx` uses `useState<Complexity | ''>('')` — no default complexity value
    - `frontend/src/components/ResultPanel.tsx` uses `String(pt)` for rendering, NOT `Intl.NumberFormat`
    - `frontend/src/index.css` content is `@import "tailwindcss";` (single line)
    - `frontend/vite.config.ts` includes both `@vitejs/plugin-react` and `@tailwindcss/vite` plugins, plus the `/api → http://localhost:8000` dev proxy
    - `frontend/index.html` has `<html lang="de">` and `<title>Software-Aufwandsschätzung</title>`
    - All 13 files exist
  </acceptance_criteria>
  <done>frontend/ tree complete with Vite-7-compatible locked versions; nginx reverse-proxy declared; Pitfall #1 (plugin version), #2 (relative URLs), #6 (SPA fallback), #17 (anti-anchoring) all respected in code.</done>
</task>

<task type="auto">
  <name>Task 2: Generate frontend/pnpm-lock.yaml</name>
  <files>frontend/pnpm-lock.yaml</files>
  <read_first>
    - frontend/package.json (created in Task 1)
    - 01-RESEARCH.md §"Frontend — Dockerfile" — the Dockerfile uses `pnpm install --frozen-lockfile`, so build fails without this lock
    - 01-PATTERNS.md §"frontend/pnpm-lock.yaml" — confirms this is a generated artifact
  </read_first>
  <action>
    Generate the deterministic dependency lockfile for the frontend. This file is required by `frontend/Dockerfile` (`pnpm install --frozen-lockfile` fails if absent).

    Run from the `frontend/` directory using corepack-managed pnpm (Node 22+ ships corepack):
    ```
    cd /Users/florianblauensteiner/Documents/claude_projects/EstimationManager/frontend && corepack enable && pnpm install
    ```

    If `corepack enable` is not available on the host (very old Node), fall back to `npm install -g pnpm@latest` then `pnpm install`. macOS Darwin 24.6.0 host typically has Node 22 via Homebrew or NVM — corepack is the canonical path.

    `pnpm install` reads `frontend/package.json`, resolves all dependencies (react 19, react-dom, vite 7, @vitejs/plugin-react 5, @tailwindcss/vite 4, tailwindcss 4, typescript 5.9, @types/react, @types/react-dom), and writes `frontend/pnpm-lock.yaml` plus populates `frontend/node_modules/`.

    Do not hand-edit `pnpm-lock.yaml`. The `frontend/node_modules/` directory is gitignored (per `.gitignore` from Plan 03) and not committed.

    CRITICAL: Verify after install that pnpm did NOT emit `ERESOLVE`, peer-dependency, or `Cannot find module 'vite'` warnings. If warnings appear, STOP — Pitfall #1 says peer warnings indicate a Vite/plugin-react version mismatch that must be fixed before lockfile generation. Re-check the pins in `package.json` from Task 1.
  </action>
  <verify>
    <automated>test -f frontend/pnpm-lock.yaml && test -s frontend/pnpm-lock.yaml && grep -q 'vite' frontend/pnpm-lock.yaml && grep -q 'react' frontend/pnpm-lock.yaml && grep -q '@vitejs/plugin-react' frontend/pnpm-lock.yaml && grep -q 'tailwindcss' frontend/pnpm-lock.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/pnpm-lock.yaml` exists and is non-empty
    - `frontend/pnpm-lock.yaml` mentions `vite`, `react`, `@vitejs/plugin-react`, and `tailwindcss` as resolved packages
    - The resolved Vite version in the lockfile begins with `7.` (NOT `8.`) — `grep -E 'vite@7\.' frontend/pnpm-lock.yaml` succeeds; `grep -E 'vite@8\.' frontend/pnpm-lock.yaml` returns nothing (allow `peer:` references to higher peer ranges)
    - The resolved `@vitejs/plugin-react` version begins with `5.` (NOT `6.`)
    - `frontend/node_modules/` exists locally for build verification (gitignored, not committed)
    - No `ERESOLVE` errors were emitted during install (Pitfall #1)
  </acceptance_criteria>
  <done>frontend/pnpm-lock.yaml generated with Vite-7-compatible dependency resolution; frontend/Dockerfile is now buildable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser DOM → user input | Untrusted form values cross here, validated server-side via Pydantic (Plan 01) |
| Frontend bundle → nginx | Static, no server-side rendering, no API key embedded |
| nginx → backend | Same trust boundary as Plan 01; SPA only generates well-formed JSON for `/api/estimates` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | npm registry packages (vite, react, react-dom, @vitejs/plugin-react, @tailwindcss/vite, tailwindcss, typescript, @types/react, @types/react-dom) | accept | slopcheck audit in 01-RESEARCH.md §"Package Legitimacy Audit" verified all 7 direct npm packages `[OK]`. No `[ASSUMED]`/`[SUS]` in this plan. |
| T-02-02 | Tampering | Frontend hardcoded base URL bypassing nginx | mitigate | `src/api/client.ts` uses relative `/api/estimates` (Pitfall #2); grep verifies no `http://localhost:8000` or `VITE_API_URL` in source. |
| T-02-03 | Information Disclosure | Vite source maps in production bundle | accept | `vite.config.ts` has `build.sourcemap: false` — no maps in `dist/`. Acceptable for v1 (local-first, no public deploy). |
| T-02-04 | DoS | Browser-side huge `pages` integer | mitigate | Backend `Field(le=10_000)` rejects with 422 (Plan 01). Frontend `<input type="number">` has no upper bound in Phase 1 — relies on server validation. Phase 2 will add `max="10000"` to the input. |
| T-02-05 | Tampering | XSS via React rendering of `String(pt)` | mitigate | React JSX auto-escapes string content; `String(pt)` is a guaranteed-safe Number-to-String coercion. No `dangerouslySetInnerHTML`. |
| T-02-SC | Tampering | corepack-fetched pnpm itself | accept | corepack is bundled with Node 22 and uses cryptographic hashes per shipped manifest. No `[SLOP]` packages. |
</threat_model>

<verification>
After both tasks:

```bash
# Structure
find frontend -type f -not -path "*/node_modules/*" -not -path "*/dist/*" | sort
# Expected (minimum): Dockerfile, nginx.conf, package.json, pnpm-lock.yaml, tsconfig.json, tsconfig.node.json,
#                     vite.config.ts, index.html, src/{main.tsx, App.tsx, index.css},
#                     src/api/client.ts, src/components/{EstimateForm,ResultPanel}.tsx
# (Plus tsconfig.app.json if the project-references variant was chosen.)

# Version locks (must NOT be 8/6/6 from slopcheck)
grep '"vite":' frontend/package.json          # → "^7.3.3"
grep '"@vitejs/plugin-react":' frontend/package.json  # → "^5.2.0"
grep '"typescript":' frontend/package.json    # → "~5.9.0"

# Pitfalls held
grep -r 'http://localhost:8000' frontend/src/    # → no match
grep -r 'CORSMiddleware' frontend/               # → no match (impossible since it's a backend concept; sanity)
grep -r 'Intl.NumberFormat' frontend/src/        # → no match (Phase 2 only)
grep 'value="" disabled' frontend/src/components/EstimateForm.tsx  # → match

# nginx reverse-proxy
grep 'proxy_pass http://backend:8000/api/' frontend/nginx.conf
grep 'try_files $uri $uri/ /index.html' frontend/nginx.conf

# Lockfile sanity
test -s frontend/pnpm-lock.yaml
grep -E 'vite@7\.' frontend/pnpm-lock.yaml      # match
! grep -E '^vite@8\.' frontend/pnpm-lock.yaml   # no match
```

The frontend image is NOT built in this plan (`docker compose up --build` happens in Plan 03). Plan 02 only ensures the source + locked deps + nginx config are correct and ready.
</verification>

<success_criteria>
- `frontend/` directory has all required files with the canonical content from 01-RESEARCH.md
- Version pins match CLAUDE.md exactly: Vite 7, plugin-react 5, TypeScript 5.9, React 19.2, Tailwind 4.3 — NONE of Vite 8, plugin-react 6, TS 6
- `frontend/pnpm-lock.yaml` exists and resolves Vite 7.x (not 8.x)
- D-01 (no default on complexity, pages + complexity inputs only), D-05 (5 TypeScript files + standard config), and INFRA-03 (`/api/*` reverse-proxy declared) all materially implemented
- No hardcoded `http://localhost:8000` URL in any frontend source file (Pitfall #2 respected)
- No `Intl.NumberFormat` in ResultPanel (Phase 1 keeps it bare per D-Specifics)
- nginx config has SPA fallback ready for Phase 2 routing (Pitfall #6 pre-emptively addressed)
</success_criteria>

<output>
Create `.planning/phases/01-skeleton-slice/01-02-SUMMARY.md` when done.
Document: which tsconfig variant was chosen (project-references with tsconfig.app.json, OR single tsconfig.json), the pnpm version used, any peer-dep warnings encountered and how they were resolved (must be zero — see Pitfall #1), confirmation that no Vite-8/plugin-react-6/TS-6 versions appear anywhere.
</output>
